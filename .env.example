# App
NODE_ENV=development
PORT=4000
FRONTEND_PORT=5173

# Database
DATABASE_URL="file:./dev.db"

# Auth (placeholder values for local development)
AUTH_JWT_SECRET=replace-with-a-long-secret
AUTH_JWT_EXPIRES_IN=1h

# Metadata provider
RAWG_API_KEY=replace-with-rawg-api-key
